---
name: gg
description: 关闭项目前的收尾操作。增量优化CLAUDE.md（排版梳理、分类归纳，标注废弃内容），并通过recall/remember同步更新agentmemory记忆，避免重复记录和跨项目串记忆。
---

读取当前会话上下文，并结合 /recall 查到的历史相关记忆，一并梳理本次
产生的历史操作流程、踩坑问题，做以下两件事：

1. 增量优化 CLAUDE.md：
   保留原有全部内容，只做排版梳理、分类归纳，把零散的规则、流程、约束
   整理到对应分区。不要删除已有信息，除非该信息已被本次对话明确推翻
   或废弃——这种情况下标注"已废弃"并简要说明原因，不要直接删掉。
   优化结构方便后续调用。

2. 同步更新 agentmemory：
   先用 /recall 查一下本次相关内容是否已经存过，只对新增或有变化的
   信息执行 /remember，避免重复记录。如果本次结论推翻了之前存过的
   内容，用 /forget 清掉旧条目，或明确写"更新：原xxx已废弃，现改为
   xxx，原因是…"。

   每条记忆分条记录，包括：
   - 所属项目/模块（作为检索锚点，避免跨项目串记忆）
   - 做了什么（完成的功能/模块，简要说明）
   - 踩过的坑（问题现象 + 根因 + 解决方案）
   - 未完成的事（下一步要做什么，当前卡在哪）
   - 关键决策及原因（比如选了A方案而不是B方案，为什么）
   每条尽量独立、具体，包含足够的上下文关键词，方便之后 /recall 精准搜到。

分工原则：CLAUDE.md 只保留"长期有效、每次都要用到"的规则和约束；
一次性的踩坑细节、具体调试过程、单次决策的详细权衡，放进 agentmemory，
不要写进 CLAUDE.md，避免文档越来越臃肿。
